00 ? +1099  // LoopStart: 读取一个数到地址 99
01 ? +2099  // 加载该数到累加器
02 ? +4107  // 如果累加器为负 (哨兵)，则跳转到地址 07
03 ? +2098  // 加载当前总和
04 ? +3099  // 将新输入的数加到总和上
05 ? +2198  // 将新的总和存回地址 98
06 ? +4000  // 无条件跳转回 LoopStart (地址 00)
07 ? +1198  // End: 显示存储在地址 98 的最终总和
08 ? +4300  // 停止程序
09 ? -99999 // 结束程序输入 (哨兵值，标记程序结束)